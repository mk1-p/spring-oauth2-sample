server:
  port: 8999

spring:

  profiles:
    include: oauth # application-oauth.yaml 파일 포함

  # H2 Setting Info (H2 Console? ???? ?? ???? ??)
  h2:
    console:
      enabled: true         # H2 Console? ???? ?? (H2 Console? H2 Database? UI? ????? ??)
      path: /h2-console     # H2 Console? Path
  # Database Setting Info (Database? H2? ???? ?? H2?? ?? ??)
  datasource:
    driver-class-name: org.h2.Driver    # Database? H2? ?????.
    url: jdbc:h2:tcp://localhost/~/development/db/oauth                 # H2 ?? ??
    username: sa                        # H2 ?? ? ??? username ?? (??? ??? ??)
    password:                           # H2 ?? ? ??? password ?? (??? ??? ??)

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true


  data:
#    # Rest Settings
#    rest:
#      base-path: /api                           # ?? ??? url
#      default-media-type: application/json      # ??? ??? ?? ??

    # Redis Settings
    redis:
      cluster:
        nodes:
          - localhost:6379





